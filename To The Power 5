
import streamlit as st

st.title("🔢 To the Power 5")

# Input from the user
number = st.number_input("Enter a number:", value=1.0)

# Calculate and display the result
if st.button("Calculate"):
    result = number ** 5
    st.success(f"{number} to the power of 5 is **{result}**")
